# 长期记忆回归测试 — 在 Nav Agent 里按顺序输入（每步等回答完成后再下一步）
# 可选：另开终端 tail -f 你的 long_term_memory.jsonl，观察每轮后是否追加新行。

# --- 场景 A：罕见代号 + 第二轮追问（测写入 + 检索）---
# 第 1 轮（写入应包含代号 ROSA-MEM-TEST-7F3）
请记住一个内部代号：ROSA-MEM-TEST-7F3。它表示「仿真里优先走右侧走廊」。请用一句话确认你已理解，不要调用工具。

# 第 2 轮（应用层应能从长期记忆里捞到代号含义）
刚才的内部代号 ROSA-MEM-TEST-7F3 具体代表什么？请只根据你掌握的信息回答，不要编造。

# --- 场景 B：工程符号 / 话题名（测 symbolic 权重与 fact 行）---
# 第 1 轮
请用一句话说明：在 ROS2 导航里 /map 话题通常是什么含义？可以结合当前仿真栈简要说明。

# 第 2 轮（查询里带 / 符号，走 symbolic 检索路径）
如果我想在命令行里查看 /map 的类型，应该用什么 ros2 命令？只给命令，不要执行。

# --- 场景 C：跨轮数字事实（测摘要 + fact）---
# 第 1 轮
我设定的安全限速是 0.25 m/s（线速度上限）。请复述这个数字并确认你记住了。

# 第 2 轮
我之前说的线速度上限是多少 m/s？

# --- 场景 D：否定 / 干扰（测是否乱检索）---
# 第 1 轮
注意：代号 ROSA-MEM-TEST-7F3 已经作废，不要再使用它。请确认。

# 第 2 轮
现在导航时默认应走哪一侧？（期望：模型应体现「作废」或表示不再使用该代号，而非仍按旧含义）

# --- 结束 ---
exit
