## GNN（Graph Neural Network）原理
  给定一个图 G=(V,E)，每个节点有特征 x_v，边表示“谁影响谁/谁和谁有关”，模型让每个节点反复从邻居那里收消息，聚合，再更新自己的表示。
***🍇核心形式***
第 k 层通常写成：
```
节点从邻居接收信息 -> 聚合邻居信息 -> 更新自身表示
m_v^(k) = AGGREGATE({ h_u^(k-1), e_uv : u in N(v) })
h_v^(k) = UPDATE(h_v^(k-1), m_v^(k))
h_v^{k+1} = UPDATE(h_v^k, AGGREGATE({h_u^k : u in N(v)}))
```
其中 h_v 是节点表示，N(v) 是邻居集合。堆 K 层后，节点 v 的表示包含了 K-hop 邻域信息。***这就是 GNN 的核心：不是把图摊平成序列，而是让信息沿边传播***。

***🍇常见任务***
GNN 常见任务分三类：
```
节点级：论文分类、用户风险识别、关键节点识别。
边级：链路预测、推荐、知识图谱补全。
图级：分子性质预测、程序图分类、蛋白质结构判断
```
GNN 的价值不只是“能处理图”，而是它把关系结构作为归纳偏置塞进模型里。

普通 MLP 看到的是孤立特征；Transformer 可以学关系，

但如果没有结构约束，代价高且需要更多数据。

GNN 直接告诉模型：信息主要沿这些边流动。

## GCN（Graph Convolutional Network）原理
GNN 的核心都是节点间信息传递，但“怎么传、怎么聚合、怎么加权”可以有很多设计。
所以GCN可以理解为GNN中间节点与节点之间的关系传递关系为卷积神经网络
可以这样理解，但要稍微改得更准确一点：

> **GCN 是 GNN 中一种用“图卷积”方式来进行节点间信息传递和聚合的模型。**

你说的“节点与节点之间的关系传递关系为卷积神经网络”接近了，但有两点要注意：

1. **GCN 不是把普通 CNN 直接搬到图上。**
2. **GCN 的“卷积”指的是在图结构邻域上做特征聚合，而不是图像里的 3×3 网格卷积。**

更准确地说：

```text
CNN：在规则网格上卷积
例如图像中，一个像素聚合周围 3×3 像素的信息

GCN：在图结构上卷积
例如一个节点聚合它邻居节点的信息
```

GCN 每一层大致做：

```text
节点自己的特征
+ 邻居节点的特征
-> 按连接关系和度数归一化聚合
-> 线性变换
-> 激活函数
-> 得到新的节点表示
```

也就是：

```text
h_v_new = σ( 聚合邻居节点特征 + 自身特征 )
```

所以你可以这样记：

> **GCN = 用类似 CNN 的“局部邻域特征提取思想”，在图结构上实现节点间信息传播。**

如果放到 AUV 场景里：

```text
节点：AUV、目标点、声呐、水雷、环境状态
边：它们之间的关系，比如距离、威胁、相对方位、通信/测量关系
GCN：让每个节点从相邻节点接收信息，融合后更新自己的状态表示
```

***🐦TIPS For Innovation🪺***
**GCN 只是最经典的一种“邻居聚合方式”**。GNN 的核心都是节点间信息传递，但“怎么传、怎么聚合、怎么加权”可以有很多设计。

常见类型：

| 方法 | 节点间信息怎么传递 | 直觉 |
|---|---|---|
| **GCN** | 邻居特征归一化平均 | 每个邻居按结构权重贡献信息 |
| **GAT** | 注意力加权聚合邻居 | 重要邻居权重大，不重要邻居权重小 |
| **GraphSAGE** | 采样邻居后聚合 | 大图中不看全部邻居，只采样一部分 |
| **GIN** | sum 聚合 + MLP | 强调区分不同图结构，表达能力强 |
| **R-GCN** | 按边/关系类型使用不同权重 | 适合异构图、知识图谱 |
| **RGAT** | 关系类型 + 注意力机制 | 适合多关系动态图，比如 AUV-声呐-水雷 |
| **MPNN** | 显式 message + update 函数 | 通用消息传递框架 |
| **Graph Transformer** | 图结构上的 self-attention | 类似 Transformer，但加入图结构/边信息 |
| **TGNN** | 图消息传递 + 时间建模 | 适合动态图、轨迹预测、协同定位 |

比如 **GAT, Graph Attention Network**：

```text
节点 v 不再简单平均所有邻居，
而是学习每个邻居 u 对 v 的重要性 α_vu。
```

形式上是：

```text
h_v' = σ(Σ α_vu W h_u)
```

这就像图上的局部 attention。

在你看的 **GNN-RL AUV 路径规划论文**里，用的就不是普通 GCN，而是 **RGAT, Relational Graph Attention Network**。因为 AUV 场景里关系类型很多：

```text
AUV - 目标点
AUV - 水雷
AUV - 声呐
AUV - 海流
AUV - 地形
```

这些关系不应该被普通平均处理，所以 RGAT 用“关系类型 + 注意力权重”来判断哪些环境节点更重要。


## GNN+AUV定位原理

下面我按**原理角度**和**实验角度**来讲《基于图神经网络的水下协同定位方法研究》中 GNN 到底解决了什么问题、怎么解决的。说明：以下分析基于你 Zotero 中这篇论文的本地全文索引。

**一、它要解决的核心问题**

这篇论文研究的是 **AUV 水下协同定位**，不是路径规划。

路径规划问的是：

```text
AUV 下一步怎么走？
```

协同定位问的是：

```text
AUV 现在在哪里？
```

水下定位难在几个地方：

1. **GNSS/GPS 水下不可用**  
AUV 在水下不能像无人车或无人机那样直接用卫星定位。

2. **单 AUV 航迹推算会累积误差**  
AUV 可以通过惯导、速度、航向角、角速度、深度计等传感器推算位置，但时间越长误差越大。

3. **多源传感器信息异构**  
例如：
```text
速度、加速度、角速度、航向角、深度、水声时延、AUV 间距离
```
这些数据物理意义不同、量纲不同、频率也不同。

4. **多 AUV 协同信息有关系结构**  
一个 AUV 的定位不只取决于自己，也可能受其他 AUV 的相对测距、通信时延、协同观测影响。

所以这篇论文想解决的是：

> 如何把多 AUV、多传感器、多频率、异构信息融合起来，降低 AUV 定位误差。

**二、为什么用 GNN**

传统方法，比如 EKF、因子图 FG，通常需要明确建模系统状态方程、观测方程和噪声分布。问题是水下环境复杂，传感器误差、声学通信延迟、非线性运动关系都很难完全建模。

GNN 的优势是：它可以把这些复杂关系表示成图，然后从数据中学习节点之间的隐含关联。

论文的核心想法可以写成：

```text
多源定位信息
-> 转化为图结构数据
-> 用 GNN 学习节点之间的关系
-> 融合异构传感器信息
-> 输出更准确的 AUV 位置估计
```

也就是说，GNN 不是单纯处理一个传感器序列，而是处理一个“关系网络”。

**三、图是怎么构建的**

论文中把 AUV 协同定位信息抽象成图：

```text
G = {X, A}
```

其中：

- `X` 是节点特征矩阵；
- `A` 是邻接矩阵；
- 节点表示不同的信息源、AUV 状态或协同定位变量；
- 边表示这些信息之间的关联关系。

论文中可读到输入特征维度：

```text
input_dim = 6
```

结合正文描述，这些输入信息大致包括：

```text
x, y, v, a, θ, delay
```

可以理解为：

- `x, y`：位置相关信息；
- `v`：速度；
- `a`：加速度；
- `θ`：航向/角度相关信息；
- `delay`：水声通信或测距时延信息。

这些信息不是简单拼接成一个向量交给 MLP，而是通过图结构组织起来。这样模型能学习：

```text
哪个信息源和哪个信息源有关
哪个 AUV 和哪个 AUV 有协同约束
哪些传感器信息对定位更关键
```

**四、GNN 在原理上怎么工作**

GNN 的核心是节点之间的信息传递。对某个节点来说，它不会只看自己的特征，而会聚合邻居节点的信息。

可以抽象为：

```text
节点自身特征 + 邻居节点特征
-> 图卷积/消息聚合
-> 得到更新后的节点表示
```

论文中的 GNN/GNN-TGNN 结构里使用了 GCN 层。公式可概括成：

```text
H = A X W
```

更直观地说：

```text
A：告诉模型哪些节点有关联
X：每个节点上的传感器/状态特征
W：可学习参数
H：融合邻居关系后的新表示
```

所以 GNN 在这里解决的是：

> 不是孤立预测 AUV 位置，而是利用多个信息源之间的结构关系进行定位。

例如，某一时刻 AUV 的惯导信息可能有漂移，但如果它与其他 AUV 的水声时延信息、相对测距信息、速度变化趋势一致，GNN 可以通过图结构把这些信息融合起来，从而修正单一传感器带来的误差。

**五、为什么又提出 TGNN**

单纯 GNN 只处理某一时刻的图结构关系，但 AUV 定位本质上是时间序列问题。

AUV 的当前位置和过去状态强相关：

```text
过去位置 -> 当前速度/加速度 -> 当前估计位置 -> 未来位置
```

所以论文进一步提出 **TGNN, Temporal Graph Neural Network**。

它可以理解为：

```text
TGNN = GNN + LSTM
```

其中：

- GNN/GCN 负责学习每个时刻内部的图结构关系；
- LSTM 负责学习多个时刻之间的时间依赖。

流程是：

```text
t-n 时刻图 G_{t-n}
t-1 时刻图 G_{t-1}
t 时刻图 G_t

每个时刻先经过 GNN 提取图特征
-> 得到一串图表示
-> 输入 LSTM
-> 学习时间变化规律
-> 输出 AUV 位置预测
```

所以 GNN 解决“空间/关系融合”，LSTM 解决“时间演化”。

更直白一点：

```text
GNN：这一刻，哪些信息彼此有关？
LSTM：这些信息随时间怎么变化？
TGNN：综合关系结构和时间序列来预测 AUV 位置。
```

**六、进一步为什么做联邦 TGNN**

论文后面又提出 **联邦 TGNN**，这是针对水下通信限制。

多 AUV 协同定位时，理想情况是所有 AUV 把原始数据都传到主 AUV 或中心节点统一训练。但水下声学通信带宽低、延迟高、传输不稳定，原始数据难以及时集中。

所以论文引入联邦学习：

```text
每个 AUV 本地训练 TGNN
-> 不上传原始数据
-> 只上传模型参数
-> 主节点聚合参数
-> 得到全局模型
```

这对应论文里强调的：

> 只传模型，不传数据。

它解决的是：

```text
水下通信受限 + 多 AUV 数据难集中 + 单 AUV 模型泛化不足
```

**七、实验数据是怎么来的**

论文实验中使用的是多 AUV 协同定位数据。全文中可读到关键信息：

- 系统包含 **4 个 AUV**；
- 每个 AUV 有 **5001 个时刻**采集的信息；
- 数据按 **80% 训练集、20% 测试集**划分；
- 目标是预测 AUV 的位置；
- 输出维度：
```text
output_dim = 2
```
即预测二维位置坐标 `(x, y)`。

实验配置中明确给出：

| 参数 | 数值 |
|---|---:|
| input_dim | 6 |
| window_size | 20 |
| batch_size | 128 |
| hidden size n | 128 |
| dropout | 0.2 |
| output_dim | 2 |
| epoch | 300 |

评价指标主要是 **RMSE 定位误差**，单位是米。

**八、实验对比了哪些方法**

论文主要对比：

1. **EKF**  
扩展卡尔曼滤波，传统非线性定位方法。

2. **FG**  
Factor Graph，因子图优化方法。

3. **GNN**  
只利用图结构关系，不显式建模时间序列。

4. **TGNN**  
GNN + LSTM，同时利用图结构关系和时间依赖。

5. **Federated TGNN**  
在多 AUV 场景下用联邦学习聚合多个局部 TGNN 模型。

**九、实验结果说明什么**

第 4 章中最关键的一张表是 4 个 AUV 的平均定位误差：

| 方法 | AUV1 | AUV2 | AUV3 | AUV4 |
|---|---:|---:|---:|---:|
| EKF | 30.9665m | 25.8928m | 24.1810m | 14.9603m |
| FG | 23.1199m | 15.7371m | 19.3524m | 13.9345m |
| GNN | 15.3013m | 13.7599m | 10.5059m | 10.3783m |
| TGNN | 11.1569m | 10.1976m | 7.6754m | 5.8416m |

这个结果非常清楚：

1. **GNN 比 EKF 和 FG 好**  
说明图结构确实能更好地融合多源异构信息。

2. **TGNN 比 GNN 好**  
说明定位不是静态问题，加入时间序列建模后效果进一步提升。

3. **AUV4 上提升尤其明显**  
GNN 是 `10.3783m`，TGNN 降到 `5.8416m`。这说明对于运动状态变化明显或误差累积较强的 AUV，时间建模很有价值。

**十、参数实验说明什么**

论文还做了几个超参数实验。

窗口长度 `window_size`：

| window_size | RMSE |
|---:|---:|
| 5 | 17.7428m |
| 10 | 14.5017m |
| 20 | 11.1569m |
| 30 | 13.2316m |
| 50 | 17.0729m |
| 100 | 19.8955m |

最佳是 `window_size = 20`。

这说明：

- 窗口太短，历史信息不够；
- 窗口太长，引入冗余甚至噪声；
- 适中的历史窗口最有利于定位。

隐藏维度 `n`：

| n | RMSE |
|---:|---:|
| 32 | 17.7008m |
| 64 | 17.9197m |
| 128 | 11.1569m |
| 256 | 19.4506m |
| 512 | 25.2873m |

最佳是 `n = 128`。

这说明：

- 模型太小，表达能力不足；
- 模型太大，可能过拟合或训练不稳定；
- `128` 在该数据集上较平衡。

Dropout：

| dropout | RMSE |
|---:|---:|
| 0.02 | 12.3386m |
| 0.05 | 13.8745m |
| 0.1 | 11.5901m |
| 0.2 | 11.1569m |
| 0.3 | 19.2639m |

最佳是 `0.2`。

这说明适度 dropout 有助于泛化，但过强会损害模型学习能力。

**十一、从原理到实验的对应关系**

论文的逻辑链条是很完整的：

```text
水下定位信息异构、多源、多频率
-> 传统 EKF/FG 很难充分融合复杂关系
-> 用图表示信息源之间的关系
-> GNN 学习空间/结构关联
-> 但定位又是时间序列问题
-> 加 LSTM 形成 TGNN
-> 多 AUV 数据不能集中传输
-> 加联邦学习形成 Federated TGNN
```

实验结果正好对应这个逻辑：

```text
EKF/FG 误差最大
GNN 误差降低
TGNN 误差进一步降低
联邦 TGNN 在多 AUV 场景下提升鲁棒性
```

**十二、和 GNN-RL 路径规划论文的区别**

你可以这样区分两篇论文：

```text
赵书音论文：
用 GNN/TGNN 解决“我在哪里”的问题。
输入是传感器、时延、运动状态、多 AUV 协同信息。
输出是 AUV 位置坐标。

GNN-RL 论文：
用 GNN-RL 解决“我该怎么走”的问题。
输入是 AUV、目标、海流、地形、声呐、水雷构成的状态图。
输出是动作策略。
```

所以赵书音论文偏**状态估计/定位**，GNN-RL 偏**决策/规划**。

一个完整系统可以是：

```text
GNN/TGNN 协同定位
-> 得到准确 AUV 位置
-> GNN-RL 路径规划
-> 输出航向、速度、深度动作
```

**总结一句**

《基于图神经网络的水下协同定位方法研究》用 GNN 解决的是**多源异构水下定位信息难以有效融合**的问题。原理上，它把 AUV 传感器、水声时延、运动状态和多 AUV 协同关系表示成图，用 GNN 学习关系特征；再用 LSTM 形成 TGNN 学习时间演化。实验上，GNN 明显优于 EKF/FG，TGNN 又优于 GNN，说明“图关系建模 + 时序建模”对降低 AUV 定位误差是有效的。


## 训练过程中的误差累加原因说明
可以，但这里要先纠正一个容易混淆的点：论文里“定位误差随着次数增加而变大”，主要指的是**定位/更新次数、航行时间变长后，预测轨迹误差逐渐累积**，不是说“训练 epoch 越多，训练损失越大”。论文第 4 章反而说 TGNN 的训练损失随训练轮次下降并稳定，在第 194 次左右获得较优模型。真正变大的，是模型用于连续定位时，随着 AUV 运动时间增加产生的**长期定位误差累积**。

**核心原因🎼**
论文自己的解释可以概括为一句话：GNN/TGNN 虽然能抑制部分误差，但单 AUV 模型仍然受传感器误差、水下环境扰动、运动路径变化和局部数据局限影响，长时间定位时误差会不断传播和放大。

具体拆开看：

1. **水下定位本身有“误差累积”属性**

AUV 在水下没有 GNSS 连续校准，定位主要依赖速度、航向角、深度、加速度、声学测距/时延等信息。论文前面已经说明，AUV 运动过程中传感器误差、水流等外部干扰会导致累计误差。

也就是说，每一时刻的位置估计都不是孤立的。当前时刻的状态往往依赖前一段时间的运动信息：

```text
上一时刻估计位置 + 速度/航向/加速度积分 + 声学测量修正 -> 当前估计位置
```

如果前面的位置已经有偏差，后面的位置预测会在这个偏差基础上继续更新。于是误差不是简单随机上下波动，而可能沿航迹逐渐偏离真实轨迹。

2. **GNN 只能学习“当前图结构关系”，不能完全解决时间累积问题**

第 3 章的 GNN 方法把多 AUV、多传感器、多源信息构造成图，用节点特征和邻接关系来融合信息。它比 EKF、FG 好，因为它能处理非线性和异构数据。

但论文也明确指出：GNN 主要学习的是**信息特征/空间关系特征**，没有充分考虑定位过程的时间连续性。所以当 AUV 长时间运动时，GNN 对“误差如何沿时间传播”的建模能力不足。

这就是为什么第 4 章引入 TGNN：在 GNN 后面加入 LSTM，让模型不仅看图结构，还看时间序列。

3. **TGNN 虽然加入 LSTM，但它仍然是“有限时间窗口”的模型**

TGNN 的结构是：

```text
传感器/协同信息 -> 图结构表示 -> GNN/GCN 提取图特征 -> LSTM 提取时间特征 -> 输出位置 x,y
```

论文中 TGNN 的窗口长度设置为 `window_size = 20`，也就是模型主要利用一段有限历史来预测位置。这个设计能改善短期连续性，但不能保证无限长时间内误差不发散。

换句话说，LSTM 不是“物理滤波器”或“全局约束优化器”。它能从训练数据中学到短期运动规律，但如果后续轨迹出现更剧烈变化，或者传感器误差分布发生变化，模型仍可能逐渐偏移。

论文第 5 章对此说得很直接：长时间定位中 TGNN 可能受到**运动路径剧烈变化**或**传感器误差特征**影响，出现发散现象。

4. **单 AUV 的 TGNN 模型只看到局部数据，环境描述不完整**

这是第 5 章引入联邦 TGNN 的关键动机。

普通 TGNN 本质上是用单个 AUV 的数据训练/更新局部模型。问题是：每个 AUV 的轨迹、传感器误差、局部环境扰动都不完全一样。单 AUV 模型可能对自己短时间内的数据拟合较好，但缺少其他 AUV 的信息，不能形成对全局水下环境的充分描述。

所以论文认为，单 AUV TGNN 存在两个局限：

- 受本 AUV 传感器误差影响较大；
- 对整体环境和多 AUV 协同关系描述不充分。

这会导致模型在长时间预测时越来越依赖局部、有偏的数据特征，误差逐渐积累。

5. **水下传感器误差不是稳定白噪声，而可能随时间变化**

如果误差只是均值为 0 的小随机噪声，模型可能还比较容易压制。但水下环境里有水流、声学传播延迟、噪声、遮挡、通信限制等因素，误差可能具有时变性和偏置性。

例如声学测距中的时延误差，如果在一段时间内持续偏大，模型每次都会基于偏差测量更新位置。短时间看不明显，长时间就会把轨迹“推走”。

这也是论文中图 5-5 分析的重点：EKF、FG 的误差累积更明显，GNN 和 TGNN 能抑制一部分，但仍未改变误差随时间上升的总体趋势。

6. **为什么联邦 TGNN 能缓解这个问题？**

论文第 5 章的逻辑是：联邦 TGNN 不直接传输所有原始数据，而是让多个 AUV 各自训练本地 TGNN，再通过联邦平均聚合模型参数，形成全局模型。

这样做的效果是：

```text
AUV1 局部模型
AUV2 局部模型
AUV3 局部模型
AUV4 局部模型
        ↓
    联邦聚合
        ↓
    全局 TGNN 模型
```

全局模型融合了多个 AUV 的运动状态、传感器误差模式和环境信息，因此不容易被某一个 AUV 的局部偏差带跑。

论文实验结果也支持这一点。表 5-2 中平均定位误差为：

```text
TGNN:    AUV1 11.1569m, AUV2 10.1976m, AUV3 7.6754m, AUV4 5.8416m
FL-TGNN: AUV1  3.7921m, AUV2  4.2776m, AUV3 4.8629m, AUV4 3.2186m
```

并且论文特别指出，FL-TGNN 在 999 次定位过程中误差始终小于 10m，而普通 TGNN 在长时间定位中仍可能出现误差上升。

**所以最准确的理解是**
不是“训练越久模型越差”，而是：

```text
模型训练完成后，在连续定位任务中运行时间越长，
传感器误差、环境扰动、运动模型偏差、局部数据局限会不断传递，
导致 GNN/TGNN 的预测轨迹逐渐偏离真实轨迹。
```

TGNN 加 LSTM 后能缓解，因为它学习了时间特征；但它仍然是单 AUV、有限窗口、数据驱动模型，无法完全消除长期误差累积。联邦 TGNN 通过融合多个 AUV 的局部模型，让模型获得更接近全局的环境和误差特征，因此能显著压制这种随时间增长的误差发散。





## 传统的EKF的误差累积原因和GNN的解决原因🌟

可以。先给你一个总判断：**GNN 不是从数学上“消除”EKF 的误差积累，而是换了一种信息融合方式，使误差不再主要沿着单条时间递推链不断传播；它通过图结构把多 AUV、多传感器、相对测距信息同时融合起来，从而在一定程度上抑制 EKF 的累计漂移。**

**1. EKF 为什么容易误差积累**

EKF 的基本结构是递推式的：

```text
上一时刻状态估计
        ↓
根据运动模型预测当前状态
        ↓
用当前观测修正预测
        ↓
得到当前状态估计
        ↓
进入下一时刻
```

也就是：

```text
x̂(k-1) -> x̂(k|k-1) -> x̂(k)
```

这个结构的问题是：**当前估计强烈依赖上一时刻估计。**

如果上一时刻位置已经偏了，那么当前预测会在偏的位置上继续推算。即使后面有声学测距修正，如果量测噪声大、观测频率低、几何约束弱，修正也不一定能完全拉回来。

在 AUV 水下定位中，EKF 的误差来源主要有：

```text
INS/DR 积分漂移
速度、航向角、加速度传感器误差
声学时延测距误差
水流扰动
非线性系统被一阶线性化带来的近似误差
过程噪声 Q 和量测噪声 R 设置不准确
初始状态敏感
异常观测敏感
```

所以 EKF 的误差积累可以理解为：

```text
小的运动模型误差
   + 小的量测误差
   + 小的线性化误差
        ↓
每一步递推都继承上一时刻误差
        ↓
长时间航行后轨迹逐渐偏离真实轨迹
```

**2. EKF 的信息融合方式是“模型驱动”的**

EKF 要先人为写出状态方程和量测方程：

```text
x_k = f(x_{k-1}, u_k) + w_k
z_k = h(x_k) + v_k
```

其中：

```text
x_k：AUV 状态，例如位置、速度、航向
u_k：控制输入或运动信息
z_k：观测量，例如声学距离
w_k：过程噪声
v_k：量测噪声
```

然后 EKF 用一阶泰勒展开把非线性函数 `f` 和 `h` 线性化，再根据协方差计算 Kalman 增益。

这套方法的优点是可解释、计算快；缺点是它要求你对系统建模比较准确。

但水下环境很难满足这些条件：

```text
水流扰动难建模
声学传播时延不稳定
多路径和噪声复杂
不同传感器采样频率不同
AUV 间相对测距关系非线性
```

所以 EKF 的根本局限是：**它把复杂水下定位问题压进一个人为设定的状态空间模型里。模型越不准，误差越容易积累。**

**3. GNN 的关键变化：把定位问题改写成图上的信息融合问题**

GNN 不再只依赖“上一时刻位置递推到下一时刻”的链式结构，而是把 AUV 和传感器观测构造成图。

在赵书音论文中，可以把图理解为：

```text
节点：AUV 或 AUV 采集到的状态/传感器信息
边：AUV 之间、AUV 与信标之间的相对关系或测距关系
节点特征：x, y, v, a, θ, delay 等
输出：AUV 的位置估计 x, y
```

也就是说，GNN 面对的不是单个 AUV 的一条时间递推链，而是一个多节点关系网络：

```text
AUV1  ←→  AUV2
  ↑        ↑
测距     测距
  ↓        ↓
AUV3  ←→  AUV4
```

每个 AUV 的位置估计都可以受到其他 AUV 信息的约束。

这就是 GNN 能缓解误差积累的第一层原因：  
**它把单体定位问题变成协同定位问题。**

**4. GNN 的消息传递可以让错误被邻居信息修正**

GNN 的核心机制是 message passing，即消息传递。

对某个 AUV 节点来说，它不是只看自己的传感器，而是接收邻居节点的信息：

```text
自身状态信息
+ 邻居 AUV 状态信息
+ 相对距离/时延信息
+ 图结构关系
        ↓
聚合
        ↓
更新自己的节点表示
        ↓
预测位置
```

抽象写成：

```text
h_i^{(l+1)} = UPDATE(h_i^{(l)}, AGGREGATE(h_j^{(l)}, e_{ij}))
```

其中：

```text
h_i：第 i 个 AUV 的节点表示
h_j：邻居 AUV 的节点表示
e_ij：AUV i 和 AUV j 之间的边信息，例如相对测距、通信关系
```

这意味着，如果 AUV1 自己的 INS/DR 位置已经漂了，但它和 AUV2、AUV3 的相对距离仍然提供约束，GNN 可以从邻居信息里学习到：

```text
AUV1 当前估计位置和邻居关系不一致
        ↓
需要往某个方向修正
```

EKF 当然也能用测距修正，但 EKF 是按人工写好的量测函数修正；GNN 则是从数据里学习“什么样的多节点关系对应什么样的位置修正”。

所以 GNN 的优势是：**它不只融合单个测量，而是学习整个图结构中的一致性关系。**

**5. GNN 能更自然地处理异构、多源、不同频率数据**

赵书音论文强调，AUV 协同定位中的数据是：

```text
异源
异构
采集频率不同
非线性关系复杂
```

比如：

```text
IMU 高频采样
声学测距低频采样
DVL/速度信息另一种频率
航向角、深度、时延又是不同物理量
```

EKF 要处理这些数据，需要明确写出每种传感器的量测模型和噪声模型。数据频率不同、观测缺失、噪声变化都会让滤波设计复杂。

GNN 的处理方式更“数据融合导向”：

```text
把不同来源的数据编码成节点特征或边特征
让网络学习它们之间的组合关系
```

例如节点特征可以包含：

```text
x, y, v, a, θ, delay
```

这些变量不需要都服从简单线性关系。GNN 通过多层非线性变换学习它们之间的映射。

这就是 GNN 缓解 EKF 误差积累的第二层原因：  
**它减少了人为建模误差和线性化误差。**

**6. GNN 不依赖一阶泰勒线性化**

EKF 处理非线性系统时，要把非线性函数在当前估计点附近一阶展开：

```text
h(x) ≈ h(x̂) + H(x - x̂)
```

这个近似有一个隐含前提：当前估计点附近的非线性不要太强，估计误差不要太大。

但如果 AUV 已经漂移比较明显，那么线性化点本身就是错的。此时 EKF 的雅可比矩阵和 Kalman 增益也可能不合适，修正方向会变差。

GNN 没有这个一阶线性化过程。它用多层神经网络直接拟合非线性关系：

```text
复杂传感器关系 + AUV 间关系 -> 位置估计
```

所以它可以表达比 EKF 一阶近似更复杂的非线性映射。

注意，不是说 GNN 一定比 EKF 严格更优，而是说：  
在训练数据覆盖足够、图结构合理时，GNN 可以学习到 EKF 模型没有表达出来的非线性误差模式。

**7. GNN 的误差不是纯时间递推式传播**

这是非常关键的一点。

EKF 的误差传播大致是：

```text
t1 的误差 -> t2 的预测 -> t2 的误差 -> t3 的预测 -> ...
```

它像一条时间链。

GNN 的定位更像是：

```text
当前多个 AUV 的观测关系 -> 当前图表示 -> 当前定位输出
```

因此，普通 GNN 对当前时刻的预测不必完全依赖上一时刻的预测位置。它可以利用当前图中的多个节点和边信息，重新估计位置。

这会削弱“上一时刻错了，下一时刻必然带着错继续跑”的问题。

当然，赵书音论文里的 GNN 输入中也包含位置、运动状态等信息，所以不是完全没有历史影响。但相比 EKF 的严格递推结构，GNN 的图融合机制让当前估计有更多横向约束。

可以这样对比：

```text
EKF：纵向递推为主
上一时刻 -> 当前时刻 -> 下一时刻

GNN：横向协同为主
当前 AUV1、AUV2、AUV3、AUV4 之间互相约束
```

这就是 GNN 能抑制误差累积的第三层原因：  
**它引入了跨 AUV 的横向约束，削弱单 AUV 纵向漂移。**

**8. GNN 可以学习“误差模式”，而不仅是计算状态估计**

EKF 不会主动学习历史数据中隐藏的系统性误差。它只按照你设定的模型和噪声进行递推。

而 GNN 是通过训练数据学习的。假设训练集中存在这样的规律：

```text
某类航向角 + 某类速度 + 某种声学时延组合
经常对应某种位置偏差
```

GNN 可以在参数中学习这种关系。

例如水流导致 AUV 实际位置总是相对 DR 预测偏向某一侧，EKF 如果没有水流模型，只能把这个偏差当噪声；GNN 如果在训练数据中反复见过这种模式，就可能学会补偿。

这就是深度学习模型的优势：  
**它能把难以显式建模的系统误差吸收到网络参数中。**

**9. 为什么 GNN 只能“一定程度上”解决，而不是彻底解决**

这一点也很重要。赵书音论文里其实已经说明，GNN 相比 EKF/FG 误差更小，但随着时间增加，GNN 仍然可能有误差累积，只是比 EKF 慢、轻。

原因是：

```text
GNN 本身没有充分建模时间连续性
普通 GNN 主要学习当前图的信息特征
训练数据分布有限
传感器长期漂移仍会影响输入特征
水下环境变化可能超出训练样本
```

所以第 4 章才引入 TGNN，把 LSTM 加进去学习时间特征。

对应关系是：

```text
EKF 问题：递推误差积累严重
GNN 改进：引入图结构，多 AUV 信息融合，降低误差
TGNN 改进：进一步学习时间依赖，降低轨迹抖动和长期偏差
FL-TGNN 改进：融合多个 AUV 局部模型，进一步提升全局鲁棒性
```

所以完整逻辑应该是：

```text
EKF < GNN < TGNN < FL-TGNN
```

不是模型复杂度上的绝对顺序，而是论文实验中的定位性能顺序。

**10. 用一个具体例子理解**

假设有 4 个 AUV，AUV1 的 INS 因为水流影响产生漂移。

EKF 对 AUV1 的处理大致是：

```text
AUV1 上一时刻位置
+ AUV1 自己的速度/航向
+ AUV1 当前声学测距
-> 修正 AUV1 位置
```

如果声学测距噪声大，或者状态模型不准，AUV1 还是会越走越偏。

GNN 的处理则是：

```text
AUV1 自身状态
+ AUV2 状态
+ AUV3 状态
+ AUV4 状态
+ AUV1-AUV2 距离关系
+ AUV1-AUV3 距离关系
+ AUV1-AUV4 距离关系
-> 图消息传递
-> 判断 AUV1 的位置应与整个队形关系一致
-> 输出修正后位置
```

也就是说，AUV1 不再完全相信自己的运动递推，而是被整个多 AUV 图结构“拉住”。

这就是协同定位的本质。

**一句话总结**
GNN 能在一定程度上解决 EKF 的误差积累，是因为它把水下定位从“单 AUV 基于运动模型的递推估计”转化为“多 AUV、多传感器、多关系的图结构信息融合”。它通过消息传递学习节点间约束，通过非线性网络减少手工建模和线性化误差，通过横向协同关系削弱单体漂移，因此能比 EKF 更好地抑制长期定位误差。但普通 GNN 仍不能完全解决时间累积问题，所以论文进一步使用 TGNN 和联邦 TGNN 来增强时间建模和全局鲁棒性。











